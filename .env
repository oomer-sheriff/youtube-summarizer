    # Set the base URL to point to our local FastAPI backend.
    BASE_URL=http://localhost:8000/v1
    
    # Set an access code to use in the UI.
    CODE=youtubesummarizer
    
    # A dummy API key is needed, but it won't be used.
    OPENAI_API_KEY=sk-dummy